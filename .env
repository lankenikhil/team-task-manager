JWT_SECRET=937045
NEXT_PUBLIC_APP_URL=http://localhost:3000

# MongoDB Atlas URI (for production deployment on Vercel)
# Replace <db_password> with your actual Atlas password
MONGODB_URI=mongodb+srv://lankenikhil25_db_user:<db_password>@cluster0.pvfnrjd.mongodb.net/team-task-manager?retryWrites=true&w=majority

# For local dev, the app reads the URI from /tmp/mongodb-memory-uri.txt
# (auto-written by mini-services/mongodb-service)
# If that file is missing AND MONGODB_URI has a placeholder,
# it falls back to starting an in-memory MongoDB within the Next.js process
